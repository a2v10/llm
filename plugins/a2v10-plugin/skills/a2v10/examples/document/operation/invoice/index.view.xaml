<Page xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml" Title="@[Invoices]">
	<Page.CollectionView>
		<CollectionView ItemsSource="{Bind Documents}" RunAt="ServerUrl">
			<CollectionView.Filter>
				<FilterDescription>
					<FilterItem Property="Period" DataType="Period"/>
					<FilterItem Property="Fragment" DataType="String"/>
				</FilterDescription>
			</CollectionView.Filter>
		</CollectionView>
	</Page.CollectionView>
	<Grid Rows="Auto,1*,Auto" Height="100%">
		<Toolbar>
			<Button Icon="Plus" Content="@[Create]"
					Command="{BindCmd Command=Create, Url='/document/invoice/edit'}"/>
			<Separator />
			<Button Icon="Edit"
					Command="{BindCmd OpenSelected, Argument={Bind Documents}, Url='/document/invoice/edit'}"/>
			<Button Icon="Delete"
					Command="{BindCmd DbRemoveSelected, Argument={Bind Documents}, Confirm='@[Confirm.Delete.Element]'}"/>
			<Separator />
			<Button Icon="Reload" Command="{BindCmd Reload}" />
			<ToolbarAligner />
			<SearchBox Value="{Bind Parent.Filter.Fragment}" TabIndex="1"/>
		</Toolbar>
		<DataGrid ItemsSource="{Bind Parent.ItemsSource}"
				  FixedHeader="True" Sort="True"
				  DoubleClick="{BindCmd OpenSelected, Argument={Bind Documents}, Url='/document/invoice/edit'}">
			<DataGridColumn Header="#" Content="{Bind Id}" Role="Id"/>
			<DataGridColumn Header="@[No]" Content="{Bind No}" Role="Fit"/>
			<DataGridColumn Header="@[Date]" Content="{Bind Date, DataType=Date}" Role="Date"/>
			<DataGridColumn Header="@[Sum]" Content="{BindSum Sum}" Align="Right" Role="Number"/>
			<DataGridColumn Header="@[Agent]" Content="{Bind Agent.Name}" LineClamp="2"/>
			<DataGridColumn Header="@[Memo]" Content="{Bind Memo}" LineClamp="2"/>
			<DataGridColumn Fit="True" Wrap="NoWrap">
				<CommandBar Visibility="Hover">
					<Button Icon="Edit"
							Command="{BindCmd Open, Argument={Bind}, Url='/document/invoice/edit'}"/>
					<Button Icon="Delete"
							Command="{BindCmd DbRemove, Argument={Bind}, Confirm='@[Confirm.Delete.Element]'}"/>
				</CommandBar>
			</DataGridColumn>
		</DataGrid>
		<Pager Source="{Bind Parent.Pager}"/>
	</Grid>
	<Page.Taskpad>
		<Taskpad>
			<Panel Header="@[Filters]">
				<PeriodPicker Value="{Bind Parent.Filter.Period}" Placement="BottomRight"/>
			</Panel>
		</Taskpad>
	</Page.Taskpad>
</Page>
